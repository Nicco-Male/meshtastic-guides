# Channels & PSK

A Meshtastic “channel” is basically:
- channel name
- PSK (pre-shared key) used to encrypt payloads
- compatible radio settings (region + LoRa preset)

If you can “see” nodes but can’t read messages, it’s often a PSK mismatch.
